## LAST FILE LOADED. Nothing may be included after this.
##
## Put anything here that has to beat an upstream definition — a
## klipper-toolchanger-easy macro, a Mainsail macro, a KAMP setting.
## Later definitions win, which is exactly why this file is named 99-.
##
## Keep a one-line comment above each override saying WHAT it overrides and
## WHY, so that after an upstream update you can tell which ones are still
## needed.
